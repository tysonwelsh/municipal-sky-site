0.3.1 — the drift stays put: scrolling no longer resets the sheet (mobile chrome resizes are ignored unless the grid really changed), a full pool holds instead of clearing itself, and Sweep At gains a "never" setting — now the default
