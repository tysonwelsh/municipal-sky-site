0.9.41 — the reveal stands on the podium: the drawings stay exactly where you ranked them and each pedestal says whose it was, the names printing on from last place up to first, with any drawing that never arrived noted underneath. A drawing is enlarged now by clicking the drawing itself — the zoom and replay icons are gone from the call, and a step tapped first, then a drawing, places it without dragging
0.9.42 — the darkroom pool gains a seventh indicator: the word mesh — Kimi's Take in word mode iframed into a swatch, every string one of the office's sixteen wait-words, commissioned (stubborn 100) to cross the whole sheet, at mockup-34's tuned 2.8-5 cells/sec and a cast scaled to the swatch (6 streams)
0.9.43 — the word mesh cast goes to 32 streams (owner call): more than a swatch can hold at once — the traffic manager places what clear spacetime it finds and the rest of the cast waits its turn
0.9.44 — the character rain leaves the darkroom: the plain-character mode of Kimi's Take is retired from the waiting rotation (the word mesh — the wait-words snaking the grid — and the word drift both stay), and the mockup shop is cleared out — every layout and design mockup deleted except the two loading-screen benches, mockup-33 (word drift) and mockup-34 (word mesh). Kimi's Take itself is untouched at /art/kimis-take/
0.9.45 — the plotter leaves the darkroom: the circuit-tracing pen is benched from the waiting rotation (the code stays, like the honest bar before it) — the pool is now the stray plea, the scatter, the wait watch, the word drift and the word mesh
0.9.46 — the word drift's letters come back to size: the port from the bench had dropped the letters' own 10px type, so each glyph took the card's text size and overflowed its 9px square, crowding the letters below it — the drift now matches the word mesh letter for letter
0.9.47 — one ink for the darkroom's lettering: the scatterword (LOADING… burst) trades the sheet's brown for the ruling blue, and the stray please/wait comes up from its deeper shade — every worded indicator (stray, scatter, drift, mesh) now writes in the same #4a628a
0.9.48 — the wait-words stop repeating themselves: in both the word drift and the word mesh a string now appears at most once per sheet — a column or stream deals its next word only from the ones not already showing (mesh engine change filed as Kimi's Take 0.14.5)
0.9.49 — the honest bar comes back from the tailor as THE TELETYPE (mockup-38, option B): the wobbled frame and hatched fill retire for a bar set entirely in type — LOADING (no ellipsis) shivering above a typed [####··········] track, with a two-digit readout that lies on its own schedule and occasionally shrugs ??%. Same seeded 45-second climb underneath, same bench: still out of the rotation until recalled
0.9.50 — the wait watch joins the blue: case, lugs, ticks, hands and pin all take the ruling's #4a628a (the old two-tone hierarchy kept), and the cream dial trades for a translucent blue wash so the graph paper shows through the face — no more one brown indicator among the blue ones
0.9.51 — the stray plea gains its ellipsis: three dots after "wait" blink into place one at a time — none, one, two, three, back to none — on a stepped 2.8-second cycle, riding along on the ricochet; the dots hold their space unlit so the bounce math never shifts
0.9.52 — the call is renamed BEST TO WORST (owner call): the ranking card's title, the rail's fifth step and the bench's final NEXT button all say it now (the rail's compact form reads B→W); internal ids are untouched
0.9.53 — the step rail retires for THE DOCKET (mockup-39): a centred strip of circled letters replaces the numbered boxes — a finished ring fills in solid graphite, the current one is red-rung on raised paper, and the fifth ring is a drawn set of scales standing for best to worst; a hairline runs between the rings, solid where you've been, dashed ahead
0.9.54 — the docket learns to spell on a desktop: on the wide bench each node becomes a PILL wearing its word — DRAWING A…D, and the scales icon with RANKING — no letter rings there (the letter would repeat the word); phones and the narrow best-to-worst sheet keep the bare rings
0.9.54 — the docket learns to spell on a desktop: on the wide bench each node becomes a PILL wearing its word — DRAWING A…D, and the scales icon with RANKING — no letter rings there (the letter would repeat the word); phones and the narrow best-to-worst sheet keep the bare rings
0.9.55 — the report path leaves the bench: the BROKEN OR OFFENSIVE checkbox and its note are gone from the grading form (owner call — space over reports); the plumbing underneath (state, handlers, wire fields, styles) is kept intact for the day it comes back
0.9.56 — the best-to-worst prints grow into their captions: MODEL A–D now sits inside the frame, pencilled over the top of the artwork (bare text, no strip below), and along each print's foot runs a wordless spark — the report card's segmented gauge showing the grade you filed on the bench; the freed strip goes back to the drawings (72→88px prints, 62→74 on small phones). The unveil's podium wears the same fittings; the results pile keeps its tape labels
0.9.57 — the stray's collision tightened: the plea's INK now kisses the walls — darkWell measures where the script hand's ink really sits inside its box (DOM-probed baselines, canvas alpha scan) and the ricochet overshoots each wall by exactly that side's air, so the letters touch flush on all four sides at any swatch size, and nothing ever clips
0.9.58 — the bench fits a phone screen: the whole survey for a drawing — exhibit, docket, four subjects, the grade and the buttons — now stands on one screen with no scrolling (the pinned drawing flexes to absorb whatever height the paperwork doesn't need, growing on tall phones instead of leaving dead paper); the scales' boxes are compacted on every width and the axis labels step up so question and answer pair off cleanly; the column head reads SUBJECT (was Axis), the last bench button reads NEXT — RANKING, and NEXT stays disabled until every scale on the panel is answered
0.9.59 — the unveil prices the drawings: each pedestal now prints what its drawing cost under the model's name — the same exact provider spend the report card states — shown only once the ranking is filed, never earlier in the flow; the pedestals stand on a little more paper to carry the line (an unpriced drawing simply shows no price, never $0)
0.9.60 — the field notes go concise (owner call): the three-paragraph intro is now one, the full inventory list is gone (the pile is the inventory; the count line stays), the colophon prose is one sentence, and the build stamp prints just the current version — the whole VERSION changelog had been printing in the colophon since the file grew past one line
0.9.61 — INSTRUCTIONS in the drawer: a torn typed sheet joins the pile as furniture (the turn button's tradition — no entry, no count line), dealt centred and in the well's upper two-thirds, always on top of the junk on every load, at a readable small tilt. Tapping it grows no specimen tag and opens no report card: the sheet lifts onto the record card's own full-screen reading layer, and Esc or a click puts it back. It drags and twists like any scrap; junk dropped on it covers it until the next visit
0.9.62 — the instructions sheet reads in the drawer: a tap now gives it the ordinary pick's in-place zoom — a bit bigger, upright, the junk still visible (dimmed) behind it — instead of the full-screen graph-paper reading layer, which is retired the day it arrived (owner call). Tap it again, tap away, or Esc puts it back
0.9.63 — THE ANALYTICS FOLDER: a closed manila folder joins the pile (tab typed ANALYTICS, a chart sliver peeking out, a faint FIELD RECORDS stamp), ordinary junk until tapped — then it opens into a small dashboard of what the drawer actually measures: a ledger strip (turns, survivals, ratings, total spend), average cost per drawing by model, first-place rate on judged turns, the grade book on the permanent 1-5 scale, the four live axes each on its own ruler, and the cumulative spend line. Every number comes from a new read-only jd-analytics endpoint over the drawer's own records; models with fewer than 3 ratings are named, never plotted. Built by an agent wave against PLAN-ANALYTICS.md with a Tufte-flavored design critique pass. Also repaired: a 0.9.61 loader bug that silently killed the report card's data handoff on every page load
0.9.64 — the analytics count the current rubric only (owner call): the quality charts — the grade book, the four axes, and the ledger's responses-rated figure — now count only ratings filed under the v17 rework onward, whoever filed them. The pre-v17 seed grades (the old Claude-only demo era) are out until their responses are re-rated; nothing is grandfathered. A card with no model clearing the n-floor now omits itself entirely (the axes join firsts and the grade book in that discipline) — a young database shows a smaller, honest folder instead of empty rulers
0.9.65 — the pick lands under a resting cursor: clicking an item (the instructions sheet included) now zooms it immediately instead of waiting for the mouse to leave it. The hover-grow rule had quietly outranked the picked zoom ever since it gained the turn-button exemption — with the cursor still on the item (which is where a cursor is when you click), hover's small grow held and the click read as doing nothing. Hover now stands down on picked items
0.9.66 — the analytics folder learns real folder anatomy (owner call + a photo-critique loop against Wikimedia photographs of actual manila folders): the ANALYTICS tab moves to first position — flush with the left edge, ONE 45-degree cut, the way 1/3-cut tabs actually tile a folder — and its type grows into the freed width, plainly readable at drawer size; the three-sided flap inset that read as an icon-in-a-tray becomes one folded sheet with a single edge; the FIELD RECORDS stamp and JD-A1 code come off the face; the peeking chart sliver becomes two fanned paper edges; and the manila shifts from golden tan to real straw. The measured 45-degree cuts, tab-depth exaggeration, corner radii and reveal strip all survived the photographs untouched
0.9.67 — the folder tab, the owner's way: inset from the left with a real run of folder edge before it (not flush — the photo-critique's first-position finding is superseded by owner call), two cuts instead of one, and both relaxed from 45 to 57.5 degrees off vertical — the splay the reference photographs actually measured — so the tab reads cut from paper rather than chiseled. ANALYTICS stays large (19px) and readable at drawer size, deliberately overhanging the narrowed top edge the way a real tab label runs close to its cut
0.9.68 — the folder opens straight onto the numbers (owner call): the dialog's masthead — the reproduced ANALYTICS tab, 'The drawer, by the numbers' and its dek — is cut entire, band and all. The folder item the visitor just tapped already says what this is; the cards say the rest. The boxed ✕ floats on the card itself, the record card's idiom
0.9.69 — the tab cut the owner wanted: steeper — 36.9 degrees from vertical, a clean 3-4-5 rise — with the two top corners rounded generously (7 units, a thumbed-round wear radius) instead of pointed; the knife-cut junctions at the folder's top edge stay near-sharp. The steepening widened the tab's top edge, so ANALYTICS returns to full 20px with clear air on both sides. (Also housekeeping: the 0.9.63–0.9.68 version lines had been appended to a stray VERSION file at the repo root — they are now in this file, where the colophon reads them)
0.9.70 — the dashboard's head becomes a slim band: cutting the whole banner (0.9.68) left the close button floating over the ledger's big figures, so the band returns at tab height — the small ANALYTICS tab on the left, the boxed close on the right, and the title and dek stay gone for good
0.9.71 — the folder deals bottom-right (owner call): the analytics folder's fresh seat comes from the well's lower-right band now instead of anywhere in the well — away from the instructions sheet's upper-centre spawn and the turn button's corner — and its layering is confirmed as ordinary junk: no always-on-top, deliberately unlike the instructions sheet
0.9.72 — the analytics folder goes to the bench (owner call, the darkroom pool's own terms): it no longer appears in the drawer, and its artwork is not even fetched — but everything stands behind the scenes: the module, the dialog, the charts, the artwork, the jd-analytics endpoint, every wire. Flipping one BENCHED flag in the folder module puts it back in the pile; nothing else needs touching
0.9.73 — the curator's bench moves into the real turn card (?bench): the re-rating backlog now runs on the same bench, rail and podium a visitor gets — an item's existing drawings dealt blind into slots, grades filed per item through jd-item-rate (ranks landing in jd_ranks), names withheld until the unveil — with a dark strip at the viewport's foot for the queue, sync truth, and scrap / rerun / skip; a phone and a desktop stay in sync through the server, and rating-bench.html is succeeded, not yet removed
0.9.74 — the assignment rides the bench: the prompt, verbatim, on every drawing's grading panel — under the exhibit on the landscape bench, just above the ratings in the portrait stack — folded to three lines with SHOW FULL PROMPT when it runs long, so Understanding the Assignment is judged with the assignment in reach
0.9.75 — the queue refuses to be cached: the host's edge cache was caught serving a previous deploy's bench queue, which would quietly break phone↔desktop sync — the endpoint now sends no-store and the strip's fetch carries a timestamp, so every queue read is the live one
0.9.76 — the prompt moves to the head of the paperwork (owner call): exhibit left, then the prompt, then the rating rows beneath a dividing rule — and the THE ASSIGNMENT label is retired, the words stand alone
0.9.77 — the prompt wears its name and runs longer: a plain PROMPT header over the words, the fold raised from three lines to seven (most prompts now show whole, no expander), HIDE replaces FOLD THE PROMPT, and the exhibit centres vertically in its column
0.9.78 — the definitions open by press and the prompt takes the head of the column: a caret beside each subject unfolds its explanation under the row (the hover tooltip retires, desktop and phone now share one gesture), the PROMPT header steps up to a section-heading voice, and the paperwork column anchors to its top — slack falls between the grade and the button, never under it
0.9.79 — the disclosure grows a handle: the caret moves ahead of the subject and doubles in size, the whole row head (label included) answers the press, and the head stops being selectable text — pointer cursor, no I-beam
0.9.80 — the prompt gets its own slip: the panel takes the dropdowns' lighter beige and hairline, setting the words off from the sheet as one more filled-in form element
0.9.81 — the gauges wait in gray: every rating row shows its segmented bar from the start — empty, grayed, ticks marking the scale's steps — filling with the pencil's color the moment a value is picked
0.9.82 — REPLAY becomes a mark: the corner button trades its word for a pencil mid-stroke, the line it's leaving trailing behind (owner's provisional pick; the word survives in the tooltip)
0.9.83 — the disclosure's caret becomes a plus, quarter-turning to an × while the explanation stands open
0.9.84 — the disclosure mark settles: a small boxed plus in the selects' own chrome, swapping to a minus while its explanation stands open
0.9.85 — the dialogs' close mark goes bare: the printed 26px box retires on the turn card, the record card and the analytics folder alike — a larger ✕ in the head's own ink, centred on the band's true height, red on hover
0.9.86 — best to worst stops shrinking on desktop: the ranking (and the unveil that carries its answer) hold the bench's 960 measure, the podium scales up to spend the room — 120px prints, taller blocks, capped and centred — and the rail keeps its word pills through the last step instead of snapping back to phone rings
0.9.87 — the specimen tags learn to write: a small fast model titles each won object in 2–5 words during the darkroom wait (jd-title.php, guarded to real turns, falling back to the old truncation on any failure) — no more run-on prompt fragments on the manila tags; two curated titles trimmed to match (germs at 400×, mason-jar poppy)
0.9.88 — the bench answers to a name: ?bench&item=<id> seats one item directly, flags or not — the door back onto the bench for a legacy favorite (rate it on the current rubric, then scripts/keep-legacy.py pins it as the drawer's display over the rerun set)
0.9.89 — the letter rain lands on the room's own floor: the pile's baseline now sits at the container's visible bottom edge instead of below the frame, so letters accumulate in view from the first landing
0.9.90 — a response can retire from display: "retired": true on a response drops it from the served payload while its row and file stay for the record — the display half of the legacy-keep exception, first used to give the Prime Fable succulent its five-response card (the keep + the four rerun comparisons, the two flawed originals stepping back)
0.9.91 — the waiting row becomes a tray on desktop: the ranking's prints grow to 140px (the unveil holds its filed 120), the row underneath gets a printed inset border, and each drawing keeps a dashed home in it — the pedestal holes' own idiom — that shows the moment its print stands up on a step, so seating the last drawing leaves four quiet homes instead of buckling the sheet; phones keep the compact podium exactly as it was
0.9.92 — one letters animation, the one that piles: the Kimi's Take word mesh is benched from the waiting pool (its streams crossed the sheet without ever piling and read as a broken letter rain), so the four indicators now deal four-into-four and the piling word drift appears in every darkroom
0.9.93 — the scatter's letters strike the walls they bounce off: the flight ran on each glyph's box — half-leading, side bearings, tracking air and all — so the letters turned back with a visible gap on every side; they now collide on their real ink, measured per glyph and, because these glyphs tumble, as the ink hull's reach in the direction of each wall at the spin angle of that instant (the stray's ink ruler, generalized to rotation). Measured: closest ink-to-wall approach falls from 7-26px to 0-4px, at or past the stray's own standard, with LOADING... still reassembling dead centre
0.9.94 — an intent is not an outcome: pressing RERUN used to retire an item from the backlog the moment the button was pressed, so a rerun that never finished — a closed window, an unrated turn, a slot that timed out — left the item in limbo, marked sent and never decided; the queue now reports whether the rerun actually landed, and an unlanded one comes back to the bench (four items return: pencil stub, subway rat, Saturn fadograph, tagged entablature). SCRAP stays absolute — it is a decision, not a pending act
0.9.95 — three owner calls: the model charts drop the legacy corpus (it was never a controlled comparison, so the preserved older drawings no longer sit in the grade book as though they had run in the four-model bracket — spend still counts them, that money was real); the system prompt learns where an object's edge is, so a tarot card or a Polaroid keeps its own printed scene and only the table under it counts as ground (harness v4-web.3); and the Saturn fadograph and pencil stub stay in the drawer as they are, scrapped from the backlog but not from the collection
0.9.96 — the bench closes on a size: every curation ends at a HOW BIG IS IT card, the taxonomy's five tiers shown as swatches drawn to their real footprints, so the size is chosen by eye against the scale itself; the choice files as an intent flag beside scrap and rerun (and flags now replace only their own kind, so a size can never un-scrap an item), a tier already on file arrives selected, and visitors never see the card — their items keep the fixed visitor tier
0.9.97 — the bench opens onto everything: the queue gains a second population — every blue-button turn whose prompt never became a drawer item, one row per distinct prompt with its best surviving run — so the 84 prompts and 314 drawings that were only ever in the database can be rated, ranked, sized, scrapped or rerun like anything else; their artwork streams from jd-gen-svg.php a drawing at a time, and any judgment already filed under today's rubric arrives prefilled for confirming
0.9.98 — the size card says what it wants: an item arriving with no size on file left "file the grades" dead with nothing explaining why, which read as a stuck card; the button now names its condition and a line under the tiers says what the choice is for
0.9.99 — the stuck card unsticks: re-rating a turn whose own pass had already filed a ranking hit jd_ranks' one-row-per-drawing key, rolled the whole batch back and reported server_error — so nothing filed and the item returned unrated on the next pass; the bench's order now replaces whatever was there, the same person's considered judgment superseding their first
0.9.100 — the scatter's side walls come in: the ink ruler was right at the top and bottom but let letters cross the left and right frame before turning (owner), and a full-bleed well clips at its wall, so ink arriving past it was sliced — the horizontal walls now hold ~8px of clearance while the vertical pair, already honest, are untouched
0.9.101 — the unsized come back: 26 items rated from a tab still running the pre-size-card script filed with no size and then counted as finished, out of the bench's reach and unpromotable; a turn is no longer done until it is sized, and one that only wants a size opens straight on that card
0.9.102 — the size actually files: the card handed it to the driver and the driver handed the ratings on without it, so every sized item filed unsized and came straight back — the one dropped argument that made the size card look like it did nothing
0.9.103 — a finished turn joins the drawer: rate all four drawings and rank them, and the object takes a real place in the collection for everyone instead of living out its life as a souvenir in one browser — served straight from the database, no deploy needed. A tick on the last card keeps one out ("keep this one out of the drawer") with every rating still recorded, and the colophon stops claiming there is no database
0.9.104 — every turn closes on the size: the card that ended a curation now ends a visitor's turn too, reading the same five tiers straight from the taxonomy — so an object arrives in the drawer at the size its maker chose rather than a default, and the keep-it-out tick moves to that last card with it
0.9.105 — housekeeping, nothing to see: the page script is six files instead of one, the endpoints share one runtime, and the things a turn says about itself (its title, its size, keep-it-out, scrap, rerun) are columns on the submission instead of coded notes on a rating — every gesture, card and animation as before
0.9.106 — admin mode: ?admin puts ADJUST RATINGS on every report card (behind the bench key, remembered per device, wrong keys throttled); the bench itself is keyed again; curated cards now show the bench's grades, ranks and size straight from the database, and a full re-rank re-points the drawer
0.9.107 — the report card is the editor: in admin mode its grades table is the scales themselves, holding what is on file, and one button saves the shown drawing's grades; the old hand-off to the bench card is gone, and items that were rerun (which the four-slot ceiling had locked out) can be rated anywhere
0.9.108 — hide and show: the admin card's HIDE FROM DRAWER takes an object out of the drawer on the spot (turns and the curated collection alike, no deploy), SHOW IN DRAWER puts it back, and the admin strip lists everything hidden
0.9.109 — the admin card's hide is a checkbox on the save row, filed with the grades; the little gauges stand beside the scales again and follow the pick; and the grades table's column is GRADE, not verdict
0.9.110 — the drawer has layers: extra-large objects lie on the floor, large ones on those, and everything smaller on top — at load and however you dig; a lifted object comes back to the top of its own layer
0.9.111 — on a phone, swipe the report card's photograph to turn to the next or previous drawing; REPLAY is a small round-again arrow now, so the photograph keeps its room; and in admin mode a saved card comes down on its own
0.9.112 — the layers are the deal at load only: the drawer opens with extra-large objects on the floor and large ones on those, and anything you lift comes to the very top again, as it always did
0.9.113 — blueprint paper: a small button in the photograph's top-right corner swaps the report card's graph paper for blueprint (pale rules on deep blue) so light artwork reads, and back; the enlargement follows and the choice is remembered on the device
0.9.114 — the enlargement is the whole photograph now: model, date, tokens, cost, the file number, DOWNLOAD SVG and a full-size REDRAW that works up there, plus the paper swap; and the colophon paragraph under the drawer is gone (the series link and build stamp stay)
0.9.115 — the report card draws its photograph on when it opens; flipping to another model's drawing just shows it (REDRAW is there when you want the show)
0.9.116 — the credit button is bigger and its lens reads PUSH / 4 MORE / JUNK on three lines; the two black bars above the label are gone (the instructions sheet and the button's spoken name say the new words too)
0.9.117 — the instructions sheet is written for a first visit: four one-sentence steps, and a × in its corner takes the sheet out of the drawer until the next load; the prompt box now asks you to describe an object you would like to see drawn as an SVG instead of suggesting a brass fish
0.9.118 — the enlargement carries only the artwork, REDRAW and DOWNLOAD SVG (plus the paper swap); the buttons have a band of their own under the drawing at every width, so nothing sits over the art on a phone
0.9.119 — a bigger close ✕ on the report card, the turn card and the analytics folder, with room reserved for it so long titles wrap to two lines instead of running under it; the enlargement gets a ✕ of its own in the corner (its paper button is gone — the plate's swap still dresses it)
0.9.120 — the ledger: an admin page (linked from the admin strip) listing every item as a table — in the drawer or not and why, which drawing shows, how far each rating has got, what the bench would offer; and the bench stops re-offering harvested reruns you already annotated (their entry annotations and ranks now count)
0.9.121 — a random device code, made in the browser the first time a turn is sent, rides each turn so one device's turns and grades can be studied together (named in the consent text, explained on the privacy page); the ledger shows it per turn and gathers a device's turns on a press
0.9.122 — the analytics folder is back in the drawer, and it holds two bar graphs now: what a drawing costs per model, and the average overall grade per model
0.9.123 — the analytics folder gains two more cards: who takes first (first place on ranked four-model turns) and the four axes (a small multiple per axis, each its own ruler); four cards, two by two
0.9.124 — the analytics folder settles on three cards: cost and grade side by side, and the four axes running the full width in one row (who takes first is off again)
0.9.125 — the four-axes panels lose the 1…4 / 1…3 labels under each ruler and the "of 4" / "of 3" in their headings, and close up the room the labels took
0.9.126 — the analytics cards are title and chart only; the line under each title is gone (a card with nothing to plot still says so)
0.9.127 — the analytics folder gains a turn-by-turn table under the axes: date, the prompt's first words, and each model's overall grade with a small gauge; five rows tall, the rest scroll
0.9.128 — on a phone the drawing being graded is as large as the report card shows it: the full width of the sheet, the survey scrolling under it
0.9.129 — the grading step's enlargement carries REDRAW and a close ✕, as the report card's does
0.9.130 — the analytics folder's grade card is titled Overall grade, and on a phone or tablet every one of the four axis panels carries the model names
0.9.131 — a second page for the drawer, /art/junk-drawer/about/: the same drawer with an explanation column beside it (drawer left, method right on a desktop; drawer first on a phone), for a reader who wants the method before the dig — placeholder copy for now; the art page itself is unchanged
0.9.132 — Understanding Assignment's definition is reworded to plain rater-facing questions (intent, attempted subject matter, resemblance to the prompt with execution set aside — taxonomy v22), everywhere it prints: the report card's disclosure, the turn survey's row, and the drawer's own how-to-read legend. The disclosure mark beside every axis name — report card and survey alike — is redrawn as two bars pinned to the box's true center instead of a "+"/"−" text glyph, whose centering rode on font metrics that drifted by device; it is also a size larger and now the same size and shape on both instruments
0.9.133 — the bench gets the blueprint too: the exhibit being graded carries the same graph/blueprint paper swap the report card has had since 2026-09-10, its corner button drawing off one shared preference (window.JD_paper, jd-core.js) so a swap on either card is what the other shows next time it opens; the enlargement wears it either way, as the report card's already did
0.9.134 — the instructions sheet is rewritten in the owner's own voice, five steps instead of four (dig around and click for a closer look; press the button and help yourself; four LLMs per prompt; grading and ranking aren't optional; it's free because you're the product) — the aria mirror text carries the same words
0.9.135 — the instructions sheet grows: the torn-paper note is 10% wider and 15% taller (the hand-torn outline scaled up, not redrawn) so the five-step rewrite reads at 14px instead of last round's cramped 13px, with real margin on every side
0.9.136 — tapping the instructions sheet zooms it further than an ordinary pick (2x instead of the shared 1.6x): it's the one object in the drawer meant to be read, not glanced at
0.9.137 — step 2 reworded: "if you want something new just press the big blue button. help yourself." — the aria mirror carries the same words
0.9.138 — Structural Coherence and Layering are redefined in plain rater-facing questions, everywhere they print (report card disclosure, turn survey, the about page's axis list, the drawer's how-to-read legend): Structural Coherence asks whether the parts are well assembled and correctly connected and whether anatomy, proportions and point of view are right — anything you would fix by moving parts on the x or y axis; Layering asks whether the parts are stacked in the right order and whether opacity, fill or unwanted background elements are wrong — anything you would fix on the z axis or by erasing background. Taxonomy v23; no rating already on file changes meaning
0.9.139 — the axis definitions are readable: the explanation that unfolds under an axis name goes up a size on both instruments (report card and turn survey), and the secondary gray both cards print it in is darkened — on the report card from 3.8:1 against the card stock, which was under the accessible floor for type that small, to 5.9:1, and on the turn card from 4.7:1 to about 7:1. Every other secondary label on the two cards rides the same two tokens and gets the same lift
0.9.140 — Je ne sais quoi asks the rater a question too: "the intangible — does it have style or is it a dud? is it tasteful or is it tacky? you know it when you see it" — with the rater's own subjective judgment openly invited, in place of the old "more than the sum of its axes" definition. All four live axes now read as questions put to the person rating, not descriptions of the rubric (taxonomy v24)
0.9.141 — /about/ becomes a walkthrough: the drawer, the rating instrument, a report card and the analytics folder each take a turn in one pinned pane while seventeen steps of explanation scroll beside them. The instrument is the real one, running the desktop succulent's four drawings with its network sealed — nothing anyone does on that page is recorded — and the walk stops on each drawing in turn, including the Kimi one whose leaves are drawn correctly and then buried behind the pot, which is what REPLAY is for. On a phone the graphic pins to the top and the steps scroll beneath it
0.9.142 — the walkthrough gets its manners: scenes cross-fade into one another instead of cutting, a metro timeline runs down the right-hand side (a station per step, grouped by scene, showing how far along you are and taking you back to any earlier moment), and the item/drawing counts are gone from the head. The two cards now render the way the drawer renders them — full width, two columns, nothing truncated — because they lay out at their real 960px and the whole card is then scaled to fit the pane, instead of being folded into the phone layout that made the succulent enormous, cut the axis labels off and pushed the report card past the bottom of the screen. A report card is visible whole, without scrolling
0.9.143 — the timeline becomes a proper metro line: one continuous rule with the stations spaced out along it, the four scenes told apart by the gaps alone, and every label gone — which takes the rail from 112px to 40px and hands the width to the explanation and the drawing beside it. Scenes now travel with the text instead of dissolving: scroll down and the graphic rides up and off while the next comes up from below, scroll back up and both reverse. The item and drawing counts are gone from the head
0.9.144 — the instrument stands up and gets its last station: the turn card on the about page is portrait now (drawing, then the prompt, then the questions), and an eighteenth step ends the scene on the podium, the four drawings off the bench and standing first to fourth. The graphic also finally moves with the page — it rests through a scene and rides up and off over the last stretch before the next one, 26x the travel it had, spent where it can be seen. Cards no longer arrive half-drawn after a scroll back up, and the drawing inside the portrait instrument is no longer missing entirely
0.9.145 — the about page's two cards share one portrait: on the instrument the drawing and the prompt now sit side by side, each half the width, with the questions full-width beneath; the report card takes the same 660px portrait — photograph left, prompt right, grades and the sibling strip beneath. And the scenes finally scroll like the text: a leaving graphic rides up at the page's own speed until every pixel of it is out of the pane, the next one following up from below, no fade — the outgoing card kept on screen as a snapshot while the real next card mounts behind it, every card photographed once at load so nothing pops in. The stepper no longer depends on animation frames (a hidden tab used to leave it dead after one dropped frame), and a card is never fitted while half-built, which is what cut a report card down to its title bar
0.9.146 — the about page's cards go back to the drawing on top and the prompt beneath it, the drawing a step larger (380px, centred) on both the instrument and the report card; the analysis scene is three cards instead of one sheet — the overall grade, then what the drawings cost, then the four axes two by two with every panel whole and keyed — and the turn-by-turn table is left off the page
0.9.147 — the about page's analytics folder looks like a folder: the manila band behind the ANALYTICS tab is gone, the tab stands on the sheet's top edge, and the sheet itself is the manila — a fixed 560px tall for all three cards, closer to square, with the grade book and the cost chart sitting at its vertical centre
0.9.148 — the two cards read as one system (owner): the report card's prompt sits in the same bordered, lighter panel the rating instrument's does; the report card's table says SUBJECT, the instrument's word; and the instrument's rows take the report card's table — a ruled column divider, a tinted column head ruled top and bottom, serif title-case subject names, and OVERALL GRADE as a tinted footer row under the heavy rule. On the about page the analytics folder's tab is bigger, its word bigger, and it reaches into the sheet so no seam shows at the join
0.9.149 — the about page's two cards carry the sheet and the bar under their drawings (the owner's pick from mockups 42–45): eight captionless frames of the drawing at successive eighths of its marks over a track with one bar per mark, play/pause at the left, MARK over N of M at the right; drag, click a frame, or step with the arrow keys to hold the drawing at any mark. The cards' old replay icons are hidden on that page
0.9.150 — the sheet and the bar is cut for THE FILMSTRIP, and it leaves /about/ for the drawer proper. Under the report card's plate and inside the rating bench's pinned plate there is now ONE row where there were two: the bar graph is gone and TWELVE frames ARE the track, butted edge to edge in one bordered container with the red caret scrubbing over the pictures, a hairline the full height at every frame boundary, and a ruler overprinted along the floor (majors at the frames, three minors between, no band and no baseline). The pictures are left unwashed and the elapsed run inks along the floor; a press anywhere on the strip scrubs continuously, and the readout is a printed fraction, the mark set over the count across a slanted solidus, where MARK over N of M used to be. It REPLACES the replay affordances it lands next to — the card's ↻ and the bench plate's pencil — and the enlargement keeps its REDRAW. A drawing with fewer marks than cells shows one cell per mark rather than repeating itself. 28px where the old control took ~62
0.9.151 — the filmstrip holds its height. Its cells carried the plate's 84:74 until now, which quietly made the frame count a height dial: a drawing with few marks got wide cells and a tall row, one with many got a short one, and no two cards agreed. The cell is a fixed 26px box the drawing is fitted into instead — the artwork keeps its own proportion and centres, so a wide cell shows the SAME thumbnail with the card's paper either side. The control is 28px, the play button's own height, whether the strip holds four frames or twelve
0.9.152 — the walkthrough goes back to two columns. On /about/ the report card and the rating instrument stop standing portrait and take their own landscape grids back: the drawing and its filmstrip on the left, the prompt opening the paperwork column on the right, grades and the sibling strip following it down. The control is lined up with the ARTWORK now rather than with the plate that holds it — the play button and the strip's right end used to overhang the picture by about twenty pixels on each side. And the mark fraction is parked: the readout is drawn on no surface (its styling is kept, one flag restores it), which hands the strip the width it was holding
0.9.153 — on the walkthrough it is the PROMPT that rides beside the drawing, and only the prompt. The first cut of the two-column card put the whole paperwork column there; the grades table, the rating rows, the buttons and the sibling strip go back under the pair at the card's full width, and the right of the band holds the prompt alone. The record card writes its prompt as two siblings — the rule and the text — which cannot share one grid cell, so about-scenes.js lifts the pair into a box of its own and the grid places that; the bench needs none of it, its prompt is already one block
0.9.154 — the walkthrough's two cards get their spacing designed rather than inherited. One scale for the page (column, row, seam) replaces the numbers that had been arrived at by moving pieces around: the scrubber gets real air above it instead of all but touching the box that holds the drawing, the seam under it comes back down, and the record's grades table and the instrument's rating rows are brought to one pitch instead of each overshooting the other in turn. The specimen block reads across — notes left, download right, file number under — and the sibling thumbnails widen so a model name sets on one line. The grades table is pinned to the band's own column so the card has a single vertical seam, and a card that grows because a reader opened an axis definition no longer shrinks away from them
0.9.155 — the placings show, and the rankings that were filed finally reach the drawer. A rank is filed against a GENERATION — one drawing — but data.php looked for ranks whose SUBMISSION already carried the item's id, and a rerun's submission never does: harvest a visitor's rerun onto a curated item and its grades came across while its ranking stayed behind. Keyed on the generation alone, the join is unnecessary — whichever submission a drawing was ranked under, it is the same drawing — and the bench's order still outranks a turn's. On the back of that, each thumbnail in OTHER MODELS, SAME PROMPT wears its placing in the top-left corner: gold, silver, bronze, a ribbon for fourth, small and held back from full strength so it annotates the drawing rather than competing with it. Fifth and beyond, and anything never ranked, wear nothing at all
0.9.156 — the placings arrive. The turn branch of data.php has always read each drawing's rank, refused the turn without it, and sorted the responses by it — and then left it behind, because the ORDER was the answer and nothing downstream had to ask the number. The thumbnails ask now, so the rank is carried out with the response: 114 filed placings across 29 turns, in the drawer since they were filed and invisible the whole time, come through as gold, silver, bronze and a ribbon
