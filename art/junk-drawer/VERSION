0.9.41 — the reveal stands on the podium: the drawings stay exactly where you ranked them and each pedestal says whose it was, the names printing on from last place up to first, with any drawing that never arrived noted underneath. A drawing is enlarged now by clicking the drawing itself — the zoom and replay icons are gone from the call, and a step tapped first, then a drawing, places it without dragging
0.9.42 — the darkroom pool gains a seventh indicator: the word mesh — Kimi's Take in word mode iframed into a swatch, every string one of the office's sixteen wait-words, commissioned (stubborn 100) to cross the whole sheet, at mockup-34's tuned 2.8-5 cells/sec and a cast scaled to the swatch (6 streams)
0.9.43 — the word mesh cast goes to 32 streams (owner call): more than a swatch can hold at once — the traffic manager places what clear spacetime it finds and the rest of the cast waits its turn
0.9.44 — the character rain leaves the darkroom: the plain-character mode of Kimi's Take is retired from the waiting rotation (the word mesh — the wait-words snaking the grid — and the word drift both stay), and the mockup shop is cleared out — every layout and design mockup deleted except the two loading-screen benches, mockup-33 (word drift) and mockup-34 (word mesh). Kimi's Take itself is untouched at /art/kimis-take/
0.9.45 — the plotter leaves the darkroom: the circuit-tracing pen is benched from the waiting rotation (the code stays, like the honest bar before it) — the pool is now the stray plea, the scatter, the wait watch, the word drift and the word mesh
0.9.46 — the word drift's letters come back to size: the port from the bench had dropped the letters' own 10px type, so each glyph took the card's text size and overflowed its 9px square, crowding the letters below it — the drift now matches the word mesh letter for letter
0.9.47 — one ink for the darkroom's lettering: the scatterword (LOADING… burst) trades the sheet's brown for the ruling blue, and the stray please/wait comes up from its deeper shade — every worded indicator (stray, scatter, drift, mesh) now writes in the same #4a628a
0.9.48 — the wait-words stop repeating themselves: in both the word drift and the word mesh a string now appears at most once per sheet — a column or stream deals its next word only from the ones not already showing (mesh engine change filed as Kimi's Take 0.14.5)
0.9.49 — the honest bar comes back from the tailor as THE TELETYPE (mockup-38, option B): the wobbled frame and hatched fill retire for a bar set entirely in type — LOADING (no ellipsis) shivering above a typed [####··········] track, with a two-digit readout that lies on its own schedule and occasionally shrugs ??%. Same seeded 45-second climb underneath, same bench: still out of the rotation until recalled
0.9.50 — the wait watch joins the blue: case, lugs, ticks, hands and pin all take the ruling's #4a628a (the old two-tone hierarchy kept), and the cream dial trades for a translucent blue wash so the graph paper shows through the face — no more one brown indicator among the blue ones
0.9.51 — the stray plea gains its ellipsis: three dots after "wait" blink into place one at a time — none, one, two, three, back to none — on a stepped 2.8-second cycle, riding along on the ricochet; the dots hold their space unlit so the bounce math never shifts
0.9.52 — the call is renamed BEST TO WORST (owner call): the ranking card's title, the rail's fifth step and the bench's final NEXT button all say it now (the rail's compact form reads B→W); internal ids are untouched
0.9.53 — the step rail retires for THE DOCKET (mockup-39): a centred strip of circled letters replaces the numbered boxes — a finished ring fills in solid graphite, the current one is red-rung on raised paper, and the fifth ring is a drawn set of scales standing for best to worst; a hairline runs between the rings, solid where you've been, dashed ahead
0.9.54 — the docket learns to spell on a desktop: on the wide bench each node becomes a PILL wearing its word — DRAWING A…D, and the scales icon with RANKING — no letter rings there (the letter would repeat the word); phones and the narrow best-to-worst sheet keep the bare rings
0.9.54 — the docket learns to spell on a desktop: on the wide bench each node becomes a PILL wearing its word — DRAWING A…D, and the scales icon with RANKING — no letter rings there (the letter would repeat the word); phones and the narrow best-to-worst sheet keep the bare rings
0.9.55 — the report path leaves the bench: the BROKEN OR OFFENSIVE checkbox and its note are gone from the grading form (owner call — space over reports); the plumbing underneath (state, handlers, wire fields, styles) is kept intact for the day it comes back
0.9.56 — the best-to-worst prints grow into their captions: MODEL A–D now sits inside the frame, pencilled over the top of the artwork (bare text, no strip below), and along each print's foot runs a wordless spark — the report card's segmented gauge showing the grade you filed on the bench; the freed strip goes back to the drawings (72→88px prints, 62→74 on small phones). The unveil's podium wears the same fittings; the results pile keeps its tape labels
0.9.57 — the stray's collision tightened: the plea's INK now kisses the walls — darkWell measures where the script hand's ink really sits inside its box (DOM-probed baselines, canvas alpha scan) and the ricochet overshoots each wall by exactly that side's air, so the letters touch flush on all four sides at any swatch size, and nothing ever clips
0.9.58 — the bench fits a phone screen: the whole survey for a drawing — exhibit, docket, four subjects, the grade and the buttons — now stands on one screen with no scrolling (the pinned drawing flexes to absorb whatever height the paperwork doesn't need, growing on tall phones instead of leaving dead paper); the scales' boxes are compacted on every width and the axis labels step up so question and answer pair off cleanly; the column head reads SUBJECT (was Axis), the last bench button reads NEXT — RANKING, and NEXT stays disabled until every scale on the panel is answered
0.9.59 — the unveil prices the drawings: each pedestal now prints what its drawing cost under the model's name — the same exact provider spend the report card states — shown only once the ranking is filed, never earlier in the flow; the pedestals stand on a little more paper to carry the line (an unpriced drawing simply shows no price, never $0)
0.9.60 — the field notes go concise (owner call): the three-paragraph intro is now one, the full inventory list is gone (the pile is the inventory; the count line stays), the colophon prose is one sentence, and the build stamp prints just the current version — the whole VERSION changelog had been printing in the colophon since the file grew past one line
0.9.61 — INSTRUCTIONS in the drawer: a torn typed sheet joins the pile as furniture (the turn button's tradition — no entry, no count line), dealt centred and in the well's upper two-thirds, always on top of the junk on every load, at a readable small tilt. Tapping it grows no specimen tag and opens no report card: the sheet lifts onto the record card's own full-screen reading layer, and Esc or a click puts it back. It drags and twists like any scrap; junk dropped on it covers it until the next visit
0.9.62 — the instructions sheet reads in the drawer: a tap now gives it the ordinary pick's in-place zoom — a bit bigger, upright, the junk still visible (dimmed) behind it — instead of the full-screen graph-paper reading layer, which is retired the day it arrived (owner call). Tap it again, tap away, or Esc puts it back
0.9.63 — THE ANALYTICS FOLDER: a closed manila folder joins the pile (tab typed ANALYTICS, a chart sliver peeking out, a faint FIELD RECORDS stamp), ordinary junk until tapped — then it opens into a small dashboard of what the drawer actually measures: a ledger strip (turns, survivals, ratings, total spend), average cost per drawing by model, first-place rate on judged turns, the grade book on the permanent 1-5 scale, the four live axes each on its own ruler, and the cumulative spend line. Every number comes from a new read-only jd-analytics endpoint over the drawer's own records; models with fewer than 3 ratings are named, never plotted. Built by an agent wave against PLAN-ANALYTICS.md with a Tufte-flavored design critique pass. Also repaired: a 0.9.61 loader bug that silently killed the report card's data handoff on every page load
0.9.64 — the analytics count the current rubric only (owner call): the quality charts — the grade book, the four axes, and the ledger's responses-rated figure — now count only ratings filed under the v17 rework onward, whoever filed them. The pre-v17 seed grades (the old Claude-only demo era) are out until their responses are re-rated; nothing is grandfathered. A card with no model clearing the n-floor now omits itself entirely (the axes join firsts and the grade book in that discipline) — a young database shows a smaller, honest folder instead of empty rulers
0.9.65 — the pick lands under a resting cursor: clicking an item (the instructions sheet included) now zooms it immediately instead of waiting for the mouse to leave it. The hover-grow rule had quietly outranked the picked zoom ever since it gained the turn-button exemption — with the cursor still on the item (which is where a cursor is when you click), hover's small grow held and the click read as doing nothing. Hover now stands down on picked items
0.9.66 — the analytics folder learns real folder anatomy (owner call + a photo-critique loop against Wikimedia photographs of actual manila folders): the ANALYTICS tab moves to first position — flush with the left edge, ONE 45-degree cut, the way 1/3-cut tabs actually tile a folder — and its type grows into the freed width, plainly readable at drawer size; the three-sided flap inset that read as an icon-in-a-tray becomes one folded sheet with a single edge; the FIELD RECORDS stamp and JD-A1 code come off the face; the peeking chart sliver becomes two fanned paper edges; and the manila shifts from golden tan to real straw. The measured 45-degree cuts, tab-depth exaggeration, corner radii and reveal strip all survived the photographs untouched
0.9.67 — the folder tab, the owner's way: inset from the left with a real run of folder edge before it (not flush — the photo-critique's first-position finding is superseded by owner call), two cuts instead of one, and both relaxed from 45 to 57.5 degrees off vertical — the splay the reference photographs actually measured — so the tab reads cut from paper rather than chiseled. ANALYTICS stays large (19px) and readable at drawer size, deliberately overhanging the narrowed top edge the way a real tab label runs close to its cut
0.9.68 — the folder opens straight onto the numbers (owner call): the dialog's masthead — the reproduced ANALYTICS tab, 'The drawer, by the numbers' and its dek — is cut entire, band and all. The folder item the visitor just tapped already says what this is; the cards say the rest. The boxed ✕ floats on the card itself, the record card's idiom
0.9.69 — the tab cut the owner wanted: steeper — 36.9 degrees from vertical, a clean 3-4-5 rise — with the two top corners rounded generously (7 units, a thumbed-round wear radius) instead of pointed; the knife-cut junctions at the folder's top edge stay near-sharp. The steepening widened the tab's top edge, so ANALYTICS returns to full 20px with clear air on both sides. (Also housekeeping: the 0.9.63–0.9.68 version lines had been appended to a stray VERSION file at the repo root — they are now in this file, where the colophon reads them)
0.9.70 — the dashboard's head becomes a slim band: cutting the whole banner (0.9.68) left the close button floating over the ledger's big figures, so the band returns at tab height — the small ANALYTICS tab on the left, the boxed close on the right, and the title and dek stay gone for good
0.9.71 — the folder deals bottom-right (owner call): the analytics folder's fresh seat comes from the well's lower-right band now instead of anywhere in the well — away from the instructions sheet's upper-centre spawn and the turn button's corner — and its layering is confirmed as ordinary junk: no always-on-top, deliberately unlike the instructions sheet
0.9.72 — the analytics folder goes to the bench (owner call, the darkroom pool's own terms): it no longer appears in the drawer, and its artwork is not even fetched — but everything stands behind the scenes: the module, the dialog, the charts, the artwork, the jd-analytics endpoint, every wire. Flipping one BENCHED flag in the folder module puts it back in the pile; nothing else needs touching
0.9.73 — the curator's bench moves into the real turn card (?bench): the re-rating backlog now runs on the same bench, rail and podium a visitor gets — an item's existing drawings dealt blind into slots, grades filed per item through jd-item-rate (ranks landing in jd_ranks), names withheld until the unveil — with a dark strip at the viewport's foot for the queue, sync truth, and scrap / rerun / skip; a phone and a desktop stay in sync through the server, and rating-bench.html is succeeded, not yet removed
