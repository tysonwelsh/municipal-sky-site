0.9.28 — the rating bench, its endpoints and the benchmark runner all print one build stamp (version · byte fingerprint · deploy time · taxonomy · harness), so "am I running the updated code?" has an answer you can read instead of guess; the bench also warns if a deploy lands mid-session
